name: "08 - Using Variables"
on:
  push:
  workflow_dispatch:
env:
  WORKFLOW_VAR: "I am a workflow env var"
  OVERWRITTEN: "I will be overwritten"
  UNDEFINED_VAR_WITH_DEFAULT: ${{ vars.UNDEFINED_VAR || 'default value' }}
jobs:
  echo:
    name: "Echo"
    runs-on: ubuntu-latest
    env:
      JOB_VAR: "I am a job env var"
      OVERWRITTEN: "I have been overwritten at the job level"
    steps:
      - name: "Print Env Variables"
        env:
          STEP_VAR: "I am a step env var"
          step-var2: "I am another step var"
        run: |
          echo "Step env var: ${{ env.STEP_VAR }}"
          echo "Step env var 2: ${{ env.step-var2 }}"
          echo "Job env var: $JOB_VAR"
          echo "Workflow env var: ${{ env.WORKFLOW_VAR }}"
          echo "Overwritten: ${{ env.OVERWRITTEN }}"
      - name: "Overwrite Job Variable"
        env:
          OVERWRITTEN: "I have been overwritten at the step level"
        run: |
          echo "Step env var: ${{ env.OVERWRITTEN }}"
  echo2:
    name: "Echo 2"
    runs-on: ubuntu-latest
    steps:
      - name: "Print Variables"
        run: |
          echo "Org var: ${{ vars.ORG_VAR || 'organization value' }}"
          echo "Org overwritten var: ${{ vars.OVERWRITTEN_VAR || 'organization value 2' }}"
          echo "Repo var: ${{ vars.REPOSITORY_VAR }}"
  echo-prod:
    name: "Echo prod"
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: "Print Prod Variables"
        run: |
          echo "Org var: ${{ vars.ORG_VAR || 'organization value' }}"
          echo "Org overwritten var: ${{ vars.OVERWRITTEN_VAR || 'organization value 2' }}"
          echo "Repo var: ${{ vars.REPOSITORY_VAR }}"
          echo "Environment var: ${{ vars.TARGET_VAR }}"
  echo-undefined:
    name: "Echo undefined"
    runs-on: ubuntu-latest
    steps:
      - name: "Print Undefined Variables"
        run: |
          echo "Org var: ${{ env.UNDEFINED_VAR_WITH_DEFAULT }}"